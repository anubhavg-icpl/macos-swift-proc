# Environment Configuration Template
# Copy this file and fill in your actual values
# DO NOT commit the filled version to version control

# PubNub Credentials (REQUIRED)
# Get these from https://dashboard.pubnub.com/
PUBNUB_PUBLISH_KEY=pub-c-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
PUBNUB_SUBSCRIBE_KEY=sub-c-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# User Identification (OPTIONAL)
# If not provided, a UUID will be generated automatically
# PUBNUB_USER_ID=your-unique-user-id

# Encryption Key (REQUIRED for production)
# Generate with: openssl rand -base64 32
# Must be at least 32 characters
DUALDAEMON_ENCRYPTION_KEY=your-strong-encryption-key-here-at-least-32-chars

# Disable encryption (NOT recommended for production)
# DUALDAEMON_DISABLE_ENCRYPTION=false

# Logging Configuration (OPTIONAL)
# DAEMON_LOG_LEVEL=info  # Options: trace, debug, info, notice, warning, error, critical
# DAEMON_LOG_FILE_PATH=/var/log/dualdaemon
# DAEMON_MAX_LOG_SIZE=10485760  # 10MB in bytes
# DAEMON_LOG_ROTATION_COUNT=5

# How to use this file:
# 1. Copy to .env: cp .env.template .env
# 2. Fill in actual values
# 3. Source before running: source .env
# 4. Or use with launchd by adding to EnvironmentVariables in plist

# For launchd integration:
# Edit /Library/LaunchDaemons/com.dualdaemon.system.plist
# Edit /Library/LaunchAgents/com.dualdaemon.user.plist
# Update the EnvironmentVariables section with your values

# Security Notes:
# - Never commit credentials to version control
# - Use secure storage for production (keychain, secrets manager)
# - Rotate credentials regularly
# - Use different credentials for dev/staging/production
